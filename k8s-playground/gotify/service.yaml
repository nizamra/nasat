apiVersion: v1
kind: Service
metadata:
  name: gotify
  namespace: gotify
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app: gotify
spec:
  type: ClusterIP
  selector:
    app: gotify
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
