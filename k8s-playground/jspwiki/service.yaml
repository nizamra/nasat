apiVersion: v1
kind: Service
metadata:
  name: jspwiki
  namespace: jspwiki
  labels:
    app: jspwiki
spec:
  type: ClusterIP
  selector:
    app: jspwiki
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
