# Cluster-wide install: the operator watches InfisicalConnection / InfisicalAuth /
# InfisicalStaticSecret CRDs in every namespace. This matches the trust level Argo CD
# itself already has in this cluster.
scopedRBAC: false
scopedNamespaces: []
installCRDs: true
