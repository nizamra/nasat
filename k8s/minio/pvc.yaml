apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc-longhorn
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-critical
  resources:
    requests:
      storage: 14Gi
