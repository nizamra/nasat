apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: nasat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: nasat/backend:latest
          env:
            - name: DB_HOST
              value: postgres
          ports:
            - containerPort: 8000
