apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      imagePullSecrets:
        - name: dockerhub-cred
      initContainers:
        # # Wait for Postgres to be reachable
        # - name: wait-for-db
        #   image: postgres:16
        #   command:
        #     - sh
        #     - -c
        #     - |
        #       getent hosts postgres
        # # Run the migration
        # - name: db-migrate
        #   image: nizamra/nasat-backend:0c392dac266dc0b951a2426b0fa3cecf7dea75df
        #   command: ["sh", "-c", "python manage.py migrate users && python manage.py migrate"]
        #   envFrom:
        #     - configMapRef:
        #         name: backend-config
        #     - secretRef:
        #         name: backend-secrets
      containers:
        - name: backend
          image: nizamra/nasat-backend:0c392dac266dc0b951a2426b0fa3cecf7dea75df
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secrets
          livenessProbe:
            httpGet:
              path: /admin/login/
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /admin/login/
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5