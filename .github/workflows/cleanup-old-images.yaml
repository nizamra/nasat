# name: Cleanup Old Docker Images

# on:
#   schedule:
#     - cron: '0 0 * * 0'  # Run weekly on Sunday
#   workflow_dispatch:  # Manual trigger
#     inputs:
#       image_name:
#         description: 'Image name to cleanup'
#         required: true
#         type: string
#       keep_versions:
#         description: 'Number of versions to keep'
#         required: false
#         default: '3'

# jobs:
#   cleanup:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Cleanup old Docker Hub images
#         env:
#           DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
#           DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
#           IMAGE_NAME: ${{ github.event.inputs.image_name || 'frontend' }}
#           KEEP_VERSIONS: ${{ github.event.inputs.keep_versions || '3' }}
#         run: |
#           # Login to Docker Hub
#           echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
          
#           # Get authentication token
#           TOKEN=$(curl -s -H "Content-Type: application/json" -X POST \
#             -d '{"username": "'$DOCKER_USERNAME'", "password": "'$DOCKER_PASSWORD'"}' \
#             https://hub.docker.com/v2/users/login/ | jq -r .token)
          
#           # Get all tags for the image
#           TAGS=$(curl -s -H "Authorization: JWT ${TOKEN}" \
#             "https://hub.docker.com/v2/repositories/${DOCKER_USERNAME}/${IMAGE_NAME}/tags/?page_size=100" | \
#             jq -r '.results[] | select(.last_updated != null) | .name + " " + .last_updated')
          
#           # Sort by date and keep only the newest KEEP_VERSIONS
#           echo "$TAGS" | sort -k2 -r | tail -n +$((KEEP_VERSIONS + 1)) | while read TAG DATE; do
#             echo "Deleting tag: $TAG (last updated: $DATE)"
            
#             curl -s -X DELETE \
#               -H "Authorization: JWT ${TOKEN}" \
#               "https://hub.docker.com/v2/repositories/${DOCKER_USERNAME}/${IMAGE_NAME}/tags/${TAG}/"
#           done
          
#           echo "Cleanup completed for ${DOCKER_USERNAME}/${IMAGE_NAME}"