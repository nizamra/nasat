apiVersion: v1
kind: Service
metadata:
  name: trilium
  namespace: trilium
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app: trilium
spec:
  type: ClusterIP
  selector:
    app: trilium
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
